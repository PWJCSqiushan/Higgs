# Copy to /srv/secrets/platform.env and chmod 600.
# Resolve and replace the placeholder before the first start; never use `latest`.
CADDY_IMAGE=caddy:2-alpine@sha256:REPLACE_WITH_VERIFIED_DIGEST
ACME_EMAIL=replace-with-备案-owner-email
