# Higgs cloud deployment

This directory contains the version-controlled, secret-free deployment skeleton.
It targets a new Tencent Cloud Lighthouse instance running Ubuntu 24.04 LTS.
Do not run these scripts on the existing rollback server.
