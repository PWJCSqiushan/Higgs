# Higgs 项目会话记忆

更新时间：2026-07-26（Asia/Shanghai）

本记录不保存 QQ 号、群号、token、API Key、登录态、聊天正文或个人绝对路径。

## 本轮目标

1. 让主人可以直接在 QQ 中管理 Higgs 的运行状态和主要策略。
2. 建立定时与事件触发的本地一致性备份。
3. 对项目文件和 Git 历史进行敏感信息防护，并发布到 GitHub 私有仓库。

## 已完成

- 新增 owner-only `/higgs` 命令，可管理启停、私聊/群聊白名单、自然触发群、关键词、连续消息等待和回复频率。
- 新增 QQ 内记忆查看、激活、隔离、失效和恢复；永久删除仍保留在本机 CLI，避免聊天误操作。
- 普通回复暂停后监听和主人命令仍保持在线，可通过 `/higgs enable` 恢复。
- 主人命令绕过普通回复限频，但主人身份仍只来自本机硬配置，模型和聊天内容不能提升权限。
- 新增 SQLite 一致性备份：启动时、配置变更后、记忆审核后、主人手动要求时以及定时间隔都会执行。
- 默认每 360 分钟备份一次，保留最近 20 份；备份清单明确排除 API Key、OneBot token、QQ 登录态和私有人格文件。
- 完整测试为 107 项，Ruff 检查与格式检查全部通过。

## GitHub 安全发布

- 远端仓库：`https://github.com/PWJCSqiushan/Higgs`。
- 仓库创建为 private，默认分支为 `main`。
- 当前快照中的真实 QQ/群标识和个人绝对路径已替换为通用占位符。
- `.env`、数据库、日志、备份、NapCat 登录态、私钥/证书、私有人格、敏感词本地表、Office 锁文件和原始设定文档均不进入 Git。
- GitHub 使用干净根历史；包含真实标识的旧本地提交历史不会上传。

## 常用入口

- 完整 QQ 命令：`apps/r-agent/docs/CHAT_COMMANDS.md`。
- 本地运维手册：`apps/r-agent/docs/OPERATIONS.md`。
- QQ 中查看状态：`/higgs status`。
- QQ 中立即备份：`/higgs backup now`。
- 本地启动：在应用目录运行 `./scripts/start_higgs.ps1`。

## 后续任务

1. 在真实 QQ 私聊和两个白名单群中验收全部 owner-only 命令。
2. 将备份目录迁移到另一块物理磁盘或加密云同步目录，以覆盖整盘故障。
3. 增加带校验和、预演和二次确认的备份恢复命令。
4. 后续如需公开仓库，必须再次完成隐私、许可证和攻击面审计。
