# R 智能体项目会话记忆

更新时间：2026-07-26（Asia/Shanghai）

本记录不保存 QQ 号、群号、token、API Key、登录态或聊天正文。

## 本轮完成

- 新增按群配置的自然对话触发模式。
- 默认群仍要求明确 @ 机器人。
- 自然触发群支持以下任一条件：
  - 明确 @ 机器人；
  - 文本包含 `higgs`，忽略大小写；
  - 文本包含汉字“你”；
  - 引用了机器人账号自己发送的消息。
- OneBot `reply` 消息段现在会解析引用消息 ID。
- 引用触发不会直接信任客户端段数据，而是通过本机 OneBot `get_msg` 查询被引用消息的真实发送者；查询失败时闭锁，不把普通引用误判为机器人引用。
- 引用其他群成员的消息不会单独触发回复。
- 新增 `R_AGENT_REPLY_NATURAL_TRIGGER_GROUPS`，且必须是群回复白名单的子集。
- `configure_qq_access.ps1` 新增 `-NaturalTriggerGroupQq` 参数，并校验自然触发群同时存在于普通群白名单中。
- 原测试群继续保留默认 @ 规则；新增测试群已加入两层白名单并启用自然触发。
- 好友私聊白名单仍为空。

## 验证

- Ruff 检查通过。
- Ruff 格式检查通过。
- Pytest 93 项全部通过。
- 配置脚本 PowerShell 语法解析通过。
- Phase 2 已重启并成功连接：`mode=live model_configured=True`。

## 下一步现场验收

在自然触发群依次测试：

1. 普通消息且不含触发词：不应回复。
2. 包含 `higgs`：应回复。
3. 包含“你”：应回复。
4. 引用机器人消息但不含上述词：应回复。
5. 引用普通群友消息且不含上述词：不应回复。
6. 回复正文不应出现 Markdown 星号。

如引用机器人消息没有触发，应检查 `phase2_reply_reference_lookup_failed` 日志以及 NapCat `get_msg` 返回结构。
