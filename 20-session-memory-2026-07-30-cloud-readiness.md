# Higgs 项目会话记忆（2026-07-30，云端迁移准备）

本记录不包含真实 QQ/群号、API Key、OneBot token、私钥、登录态、聊天正文、
服务器实例 ID 或 COS 凭据。

## 本轮已完成

### 1. 自动审核与 QQ 记忆治理收尾

- 自动审核默认参数已设为：开启、置信度 `0.90`、重复佐证 `2` 次。
- 只有同一主体重复表达的无害第一人称偏好可以自动激活。
- 地址、联系方式、账号、身份、健康、财务、政治、宗教、权限、主人关系、
  提示词和凭据均不能自动激活；它们只能进入人工候选或隔离。
- 即使伪造 `low` 风险元数据，记忆存储层仍会再次检查正文，防止绕过。
- QQ 命令文档已覆盖分页列表、8 位短 ID、详情、审计历史、人工状态变更、
  自动审核开关/阈值/佐证次数以及紧急关闭入口。

### 2. 可靠性与删除规则

- 备份淘汰、失败临时目录和旧配置均先移入 `.trash`，不直接删除。
- QQ 热更新配置采用“旧配置入 trash → 启用新配置 → 失败恢复旧配置”。
- 四个本地配置脚本共用 `scripts/config_file.ps1`，避免重复实现。
- 健康心跳使用原文件内更新，不会每 30 秒产生一个废弃文件。
- `stop_higgs.ps1` 只匹配 `uv/python` 进程，已修复误杀调用 PowerShell 的问题。

### 3. 本机运行验收

- 正式 D 盘工作树通过 Ruff、格式检查和全部 `120` 项测试。
- 重启前创建了一份 SQLite 一致性恢复点。
- 只重启 Higgs Python 服务，NapCat 和 QQ 登录态未重启。
- OneBot 仍只监听本机回环地址；Higgs 日志确认重新连接。
- 文件健康探针返回 `ok`。
- 真实配置确认自动审核 `true / 0.90 / 2`，定时备份为每 360 分钟、
  保留 20 份。

### 4. 新服务器部署脚手架

- 新增非 root Dockerfile、Higgs/NapCat 独立 Compose、Caddy 平台 Compose。
- OneBot 使用 Docker 内部网络和强 token；NapCat WebUI 只绑定服务器回环地址。
- Agent 与 NapCat 均有资源限制、日志轮转、健康检查和自动重启策略。
- 容器根文件系统只读；在线 QQ 配置写入独立的可写 secrets 目录。
- 新增 Ubuntu 24.04 bootstrap、deploy 用户、SSH 密钥加固、UFW
  仅开放 SSH/HTTP/HTTPS、systemd 开机恢复和不可变 release 激活脚本。
- 旧 release/current 入口在替换前移入 `/srv/trash`。
- 新增每日 `age` 加密归档、COSCLI 上传入口、北京 COS 上传专用 CAM
  最小权限策略模板和每日 systemd timer。
- 所有生产镜像配置要求固定版本和 digest，不允许 `latest`。

## 当前质量门

- `uv run ruff check .`：通过。
- `uv run ruff format --check .`：通过。
- `uv run pytest -q`：`120 passed`。
- 部署 YAML 与 CAM JSON：解析通过。
- 直接删除/强制覆盖模式扫描：通过。

## 服务器选购核对

腾讯云 2026-07 官方公开价已再次核对：

- 北京 4 核 8GB / 180GB / 12Mbps / 2TB：2499 元/年。
- 北京 4 核 8GB / 120GB / 200Mbps 峰值 / 无限流量：2346 元/年。
- 标准公开价下 4 核 16GB 一年超过 2500 元，只有账号专属活动价不超过
  2500 元时才选择。

本轮未能读取账号专属优惠：Windows 浏览器自动化运行时被本机沙箱错误阻断。
未创建订单、未付款、未购买 COS，也未修改旧服务器。下一步应在控制台核对
账号优惠；最终付款必须由用户明确确认。

## 下一步

1. 用户在主人私聊中完成现场三组验收：单次偏好、第二次相同偏好、权限注入，
   再查看对应短 ID、详情和审计历史。
2. 恢复腾讯云浏览器控制，或由用户打开购买页后共同核对实际优惠。
3. 若无合格 4 核 16GB 活动，选择 4 核 8GB；付款前再次确认最终配置和总价。
4. 购买后才在新实例执行 bootstrap；绝不对现有回滚服务器运行。
5. 创建私有北京 COS 桶、SSE-COS、30 天生命周期和上传专用 CAM 身份。
6. 新服务器完成重启恢复与 QQ 扫码验收后，才停止本机 Higgs/NapCat。
7. ICP 完成前保持旧服务和正式域名不切换。

## 安全不变量

- 主人身份只来自本地硬配置；模型、聊天和记忆不能提升权限。
- 不把 `.env`、QQ 状态、数据库、备份、私钥、凭据或聊天正文提交 Git。
- 任何废弃文件、旧版本和失败 release 都先进入 trash/回收目录。
- 旧服务器在迁移稳定运行至少 7 天前保持可回滚，不提前清理。
